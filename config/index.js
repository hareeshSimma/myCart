module.exports = {
    // secret: process.env.NODE_ENV === 'production' ? process.env.SECRET : 'secret'
    //secret:24081994
    secret: process.env.NODE_ENV === 'production' ? 'hareeshsimma' : 'hareeshsimma'
  };
  